---
'@storycap-testrun/browser': patch
---

Draw sticky and fixed elements once in a stitched full-page capture, at the position they would hold in a viewport tall enough to show the whole story, instead of repeating them in every chunk (issue #297).
